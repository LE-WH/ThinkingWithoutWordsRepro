# Training dependencies — verified on torch 2.9.1+cu128 / Python 3.12 / Ubuntu 24.04.
# torch, cuda runtime, and vllm are installed explicitly by scripts/setup.sh.
# Do NOT add torch or vllm here.

transformers>=4.51,<6
accelerate>=1.13,<2
datasets>=2.20,<6
peft>=0.19,<1
huggingface_hub>=0.27
sentencepiece
protobuf
einops
math_verify

# Full-FT path (optional; ZeRO-3 offload)
deepspeed>=0.15,<1

# Utilities
tqdm
