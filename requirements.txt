# Pinned to versions verified on torch 2.11.0+cu130 / Python 3.14 / Ubuntu 24.04.
# torch + CUDA come from the system / base image — do not list here.

transformers>=4.51,<6
accelerate>=1.13,<2
datasets>=2.20,<5
peft>=0.19,<1
huggingface_hub>=0.27
sentencepiece
protobuf
einops
math_verify

# Inference (eval + optional gen_traces port)
vllm>=0.20,<1

# Full-FT path (optional; CPU-Adam offload may fail to build on some
# torch+CUDA combos — see docs/SMOKE_REPORT.md).
deepspeed>=0.19,<1

# Utilities
tqdm
